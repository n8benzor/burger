<div class="grid">

    <header>
        <div class="section">
            <h1 id="title"><i class="fas fa-hamburger" type="submit" id="devourBurger"></i> E A T - D A - B U R G E R <i
                    class="fas fa-hamburger" type="submit" id="devourBurger"></i></h1>

            <p>_ __ ______________________________________ __ _</p>
        </div>
    </header>





    <aside class="sidebar-left">
        <div class="section">
            <h3 id="burger-list">YOUR BURGERS</h3>
            <p>_ ______________ _</p>
            <br>
            <ul class="list-group">
                <span>
                    {{#each burgers}}
                    {{#unless this.devoured}}

                    <li>
                        {{name}}

                        <button class="change-burger" data-id="{{id}}" data-newsleep="{{devour}}">
                            {{#if devoured}}SLEEP TIME!{{else}}<i class="fas fa-hamburger" id="devourBurger"></i> DEVOUR
                            IT!{{/if}}
                        </button>
                    </li>
                    {{!-- <li class="list-group-item">

                <p>{{this.name}}</p>


                <form action="api/burgers/devoured/{{this.id}}?_method=PUT" method="POST">

                    <input type="hidden" name="devoured" value="true">
                    <button ><i class="fas fa-hamburger" id="devourBurger"></i> Devour it!</button>
                    
                </form>

            </li> --}}
                    {{/unless}}
                    {{/each}}
                </span>
            </ul>
        </div>
    </aside>

    <article>
        <div class="section">
            <h3>CREATE A BURGER</h3>
            <img src="assets/images/bwburger.png" id="burger-logo">
            <form action="api/burgers" method="POST">
                <input type="text" id="name" name="name" placeholder="Enter Your Burger Here">
                <button type="submit" class="addBurger">Add it!</button>
            </form>
        </div>
    </article>


    <aside class="sidebar-right">
        <div class="section">
            <h3 id="burger-list">DEVOURED BURGERS</h3>
            <p>_ ____________________ _</p>
            <br>
            <ul class="list-group">
                {{#each burgers}}
                {{#if this.devoured}}
                <li class="list-group-item">
                    <p>{{this.name}}</p>
                </li>
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </aside>

    <footer>
        <p><a href="/api/burgers">API Burgers</a> | <a href="https://github.com/n8benzor/burger">GitHub Repo</a></p>
    </footer>

</div>